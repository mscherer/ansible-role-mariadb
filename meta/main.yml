---
galaxy_info:
  author: Michael Scherer
  description: Install mariadb (or MySQL)
  company: Red Hat
  license: MIT
  min_ansible_version: 1.2
  # Only tested on these platform
  # It doesn't mean it doesn not work anywhere else,
  # just no time to test.
  # I would welcome patches
  platforms:
  - name: EL
    versions:
    - 6
    - 7
  - name: Fedora
    version:
    - 21
    - 22
  categories:
  - system
dependencies: []
  # List your role dependencies here, one per line. Only
  # dependencies available via galaxy should be listed here.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.

